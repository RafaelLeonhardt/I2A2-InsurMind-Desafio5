# STATE

## Decisions

### AD-001
- **Decision**: DuckDB schema evolves through numbered `.sql` migration files tracked in a `schema_migracoes` ledger table; each migration runs in its own transaction, applied only by an explicit CLI command (`composicao.inicializador`) — the server never auto-migrates on boot, it only refuses to start if the recorded version is pending or newer than the code knows.
- **Reason**: Makes schema evolution auditable and safe to reason about (DuckDB fully supports transactional DDL); an explicit trigger avoids a server accidentally mutating a shared demo database on every restart.
- **Trade-off**: One extra manual step (`uv run ... composicao.inicializador`) documented in the README, instead of "just works on boot."
- **Scope**: All backend persistence work (`adaptadores/persistencia`), every future story that adds or evolves DuckDB tables.
- **Date**: 2026-08-28
- **Status**: active

### AD-002
- **Decision**: A generic `chaves_idempotencia` table (`chave`, `operacao`, `hash_requisicao`, `resposta_status`, `resposta_corpo`), scoped by operation name, is the standard `Idempotency-Key` mechanism for every mutable `POST` endpoint — not a per-endpoint table.
- **Reason**: `ARCHITECTURE-SPINE.md` AD-7 already mandates idempotency on every mutable `POST`; building one small reusable store now avoids every future story (coleta manual, geração, revisão, simulação) reinventing it.
- **Trade-off**: Slightly more generic/abstract than a restore-specific table would be, for a benefit only later stories cash in.
- **Scope**: Every `adaptadores/api` router exposing a mutable `POST`.
- **Date**: 2026-08-28
- **Status**: active

### AD-003
- **Decision**: Story 1.2's frontend restore surface uses a plain `fetch` call (`src/frontend/src/api/dadosSinteticos.ts`) and a hand-rolled `Modal` component, built ahead of História 1.5's OpenAPI-generated client and História 1.4's navigation shell.
- **Reason**: Story 1.2's acceptance criteria require a real, demoable confirmation-modal UI now; waiting for 1.4/1.5 would mean shipping this story backend-only and reopening its already-approved spec.
- **Trade-off**: The `fetch` call and page wiring will need rework once 1.5 introduces `openapi-typescript`-generated types and 1.4 introduces real navigation/routing — accepted as a small, contained cost since the `Modal` component itself is generic and reusable.
- **Scope**: `src/frontend/src/api/`, `src/frontend/src/componentes/Modal.tsx`, `src/frontend/src/funcionalidades/dados-sinteticos/` until Histórias 1.4/1.5 land.
- **Date**: 2026-08-28
- **Status**: active

### AD-004
- **Decision**: `dominio/estados_execucao.py` defines the canonical `EstadoExecucao` enum and terminal/non-terminal classification now, ahead of `ExecucaoPreventiva`'s actual behavior (Epic 2/3).
- **Reason**: Story 1.2's restore guard (DW-002) needs a real, testable notion of "non-terminal execution"; defining the state names now, matching `ARCHITECTURE-SPINE.md` AD-4's canonical diagram exactly, means Epic 2/3 extend an existing enum instead of introducing a second one that has to be reconciled.
- **Trade-off**: A domain module exists before the workflow it describes is implemented — a future reader needs this note to understand why.
- **Scope**: `dominio/estados_execucao.py`, `execucao_preventiva` table, every future story that transitions or reads execution state.
- **Date**: 2026-08-28
- **Status**: active

### AD-005
- **Decision**: DuckDB tables do not declare `REFERENCES` foreign-key constraints. Relationships between tables (e.g. `apolices.segurado_id → segurados.id`) are documented as logical foreign keys in the migration file's comments and in `adaptadores/persistencia/README.md`, not enforced by the schema.
- **Reason**: DuckDB does not defer foreign-key checks within a transaction. With `REFERENCES` declared, it refuses to delete a referenced table in the same transaction its children are deleted, and refuses to update a `LIST` column (`apolices.coberturas`) on a referenced table (`duckdb/duckdb#13819`). That makes the single-transaction delete-and-reinsert restore required by `spec.md`'s `SEED-09` impossible to implement as designed.
- **Trade-off**: The database itself no longer catches an orphaned/dangling reference at write time; integrity depends on the versioned seed being the only writer of these tables (true for this story) and on future stories that mutate them respecting the documented relationships without a DB-level backstop.
- **Scope**: `adaptadores/persistencia/migracoes/`, every future migration that adds a table with a relationship to an existing one, and any future story that needs transactional delete/replace semantics against related tables.
- **Date**: 2026-08-28
- **Status**: active

### AD-006
- **Decision**: All in-process periodic/background work in the backend runs as a single `asyncio` task started in the FastAPI `lifespan` (e.g. `composicao/agendador_meteorologico.py`), never a new scheduling dependency (APScheduler, Celery, etc.) or an OS-level cron.
- **Reason**: `ARCHITECTURE-SPINE.md` AD-7 already mandates a single in-process runner for asynchronous work; a bare `asyncio` loop reuses that same mechanism for scheduled INMET collection (História 2.1) with zero new dependencies, and keeps `README.md`'s "starts without hidden manual steps" promise intact.
- **Trade-off**: Doesn't survive a process restart or scale to multiple workers — accepted because the PoC is explicitly single-process (AD-2).
- **Scope**: Any future story that needs periodic or scheduled backend work (weather polling, retries, cleanup).
- **Date**: 2026-08-30
- **Status**: active

### AD-007
- **Decision**: A dedicated, versioned mapping table (`areas_monitoradas_inmet`) maps a real, monitored INMET station code to the synthetic `codigo_ibge_area` values used by the demonstration's `segurados`/`apolices` (e.g. `9990001`, `9990002`) — the domain never assumes real INMET geography aligns with the synthetic dataset's area codes.
- **Reason**: `semeador.py` seeds policyholders with fictional area codes, not real IBGE codes; without an explicit mapping, a real INMET event could never match a synthetic eligible policyholder, silently breaking the "real weather → synthetic eligibility" demo path required by História 2.1/2.5.
- **Trade-off**: Adds one small configuration table that must be kept in sync with whatever areas the synthetic dataset uses; acceptable because the demo's area set is small and fixed.
- **Scope**: `adaptadores/persistencia/migracoes/0002_meteorologia.sql`, `adaptadores/meteorologia/`, any future story that normalizes real external geography into the synthetic demonstration's area codes.
- **Date**: 2026-08-30
- **Status**: active

### AD-008
- **Decision**: Optimistic concurrency via a `versao INTEGER` column + a client-supplied `versao_esperada` is the only concurrency-control mechanism for any mutable domain aggregate — never a pessimistic lock, never `atualizado_em` used as a version proxy.
- **Reason**: Independently re-derived and applied identically four times (`RepositorioExecucaoPreventiva` in 2.2, `RepositorioRegras` in 2.4, `RepositorioMensagens` in 3.2, `RepositorioSegurados` in 5.6) — each design cited the previous one as precedent rather than a shared source. Formalizing it once prevents a future implementer (or a context-less sub-agent) from inventing a divergent mechanism for the next mutable table.
- **Trade-off**: None beyond what each individual adoption already accepted — this AD only centralizes an already-uniform practice, it doesn't change behavior anywhere.
- **Scope**: Every future table backing a mutable domain aggregate (`CREATE TABLE` with an `UPDATE` path reachable from the API).
- **Date**: 2026-08-31
- **Status**: active

### AD-009
- **Decision**: "Retry after a terminal failure" always creates a new, correlated `ExecucaoPreventiva` (`execucao_origem_id` pointing at the terminal execution, a fresh `execucao_id`, its own `Idempotency-Key`) — it never reopens, mutates, or reuses the terminal execution's row.
- **Reason**: Independently re-derived three times for three different terminal states (`falhou_coleta` in 2.2, `falhou_preparacao_ia` in 3.1, `falhou_simulacao` in 3.6), each citing the prior as precedent. AD-7 (`ARCHITECTURE-SPINE.md`) already mandates this at the architecture level; this AD makes the *exact shape* (which fields, which snapshot-validity check before creating the row) a single citable pattern instead of three parallel descriptions.
- **Trade-off**: None new — same three-time-validated shape, just centralized.
- **Scope**: Any future terminal execution state that a user can explicitly retry from.
- **Date**: 2026-08-31
- **Status**: active

### AD-010
- **Decision**: Domain-content deduplication (as opposed to command-level idempotency, AD-002) is enforced by a DB `UNIQUE` constraint on the natural content key, written via insert-or-noop (`INSERT ... ON CONFLICT DO NOTHING` or equivalent) — never an application-level "check then insert" that races.
- **Reason**: Applied identically five times for five different content types (`eventos_meteorologicos` in 2.2, `elegibilidades_historicas` in 2.5, `mensagens` in 3.2, `entregas_simuladas` in 3.6, `visualizacoes_comunicado` in 4.3). AD-002 already covers *replaying the same command*; this AD covers the separate concern of *two different commands producing the same domain fact* (e.g. a real coleta and a retried coleta both observing the same weather event). Naming both prevents either concern from being solved by the other's mechanism.
- **Trade-off**: Requires picking the correct natural key up front per table (already done correctly five times); a wrong key silently under-deduplicates. No behavior change from formalizing it.
- **Scope**: Any future table where two independent operations could legitimately produce the same domain fact.
- **Date**: 2026-08-31
- **Status**: active

### AD-011
- **Decision**: Any endpoint that resolves a resource scoped to an owner (`segurado_id`, `execucao_id`, etc.) returns an identical "not found" response whether the resource doesn't exist at all or exists but belongs to a different owner — the response body, status code, and message never differ between the two cases.
- **Reason**: Applied identically six times across the Segurado-facing and Marina-facing surfaces (4.2, 4.3, 5.2, 5.3, 5.4, 5.5), each design independently restating the same non-enumeration rationale. Centralizing it as a project-wide rule (rather than six local restatements) makes it the default a future endpoint must actively opt out of, not something each new history has to remember to add.
- **Trade-off**: None — this is a security/privacy hardening with no functional cost; it only forbids a more informative (and leakier) error message.
- **Scope**: Every future API endpoint that looks up a resource by ID scoped to a caller-supplied owner ID.
- **Date**: 2026-08-31
- **Status**: active

### AD-012
- **Decision**: A correlated retry execution that starts in `aguardando_geracao` (retry from `falhou_preparacao_ia` or `falhou_simulacao`) materializes its own copies of the origin's eligibility snapshot rows: every `elegibilidades_historicas` row of the origin (included and excluded) is duplicated with a fresh `id` and the new execution's `execucao_id`, identical snapshot content, in the same transaction that creates the new execution. The new execution never references the origin's eligibility rows directly.
- **Reason**: `contextos_agente.elegibilidade_id` (3.1) and `mensagens (elegibilidade_id, canal)` (3.2) carry `UNIQUE` constraints, and an origin that reached `simulando` already owns a context and a message per included eligibility — a retry that "redoes preflight, geração, crítica" against the same rows violates both on the first insert. `elegibilidades_historicas`'s `UNIQUE` includes `execucao_id` (2.5), which is exactly what makes per-execution copies legal. This operationalizes the snapshot copy that `ARCHITECTURE-SPINE.md` AD-11 requires but no design had made concrete. (Independent review, finding A1.)
- **Trade-off**: Snapshot rows are duplicated per retry — negligible at PoC volume, and it buys full per-execution audit (each execution's public preview and timeline stay self-contained).
- **Scope**: `ServicoPreflightIA.solicitar_nova_tentativa` (3.1), `ServicoSimulacao.solicitar_nova_tentativa` (3.6), and any future retry entering `aguardando_geracao`. Retries from `falhou_coleta` (2.2) are out of scope — they restart at `coletando` with no eligibility to copy.
- **Date**: 2026-08-30
- **Status**: active

### AD-013
- **Decision**: `granizo` events are synthetic-only in the MVP. `real_inmet` provenance is reserved for `chuva_intensa` (derived from station precipitation readings); `tipo = granizo` enters exclusively through the synthetic contingency scenario (2.2, `AdaptadorCenarioSintetico`), always labeled `proveniencia = 'sintetico'`.
- **Reason**: Hourly readings of INMET automatic stations (the endpoint chosen in 2.1) expose precipitation, temperature, wind, pressure, humidity — there is no hail field, so the "real granizo sample" 2.1 T4 originally demanded is unobtainable, and 2.3's "granizo é binário na fonte pública" rationale was unverified. Switching to INMET's alert endpoint would add a second unproven external contract days before the deadline. (Independent review, finding A2.)
- **Trade-off**: The "real weather → eligible policyholder" demo path exists only for chuva_intensa; the granizo end-to-end scenario (5.8) runs on the labeled synthetic scenario — explicit and acceptable for a PoC whose E2E suite already runs without real external integration.
- **Scope**: 2.1 normalizer and fixtures, 2.3 relevance rationale, 2.2 synthetic scenario, 5.8 granizo E2E scenario, and any future event type the chosen INMET endpoint cannot observe.
- **Date**: 2026-08-30
- **Status**: active

### AD-014
- **Decision**: The synthetic-data restore returns the database to the complete versioned initial state. Inside its single transaction it wipes, catalog-driven (enumerating tables from the DuckDB catalog), every table except an explicit allowlist — `schema_migracoes` plus versioned-config tables (today only `areas_monitoradas_inmet`) — then reseeds the seeded tables as before. New tables are covered automatically; only a new versioned-config table must join the allowlist in the story that creates it.
- **Reason**: Epic 1's restore deleted only the seeded tables. Epics 2–4 add 14 execution-produced tables that would survive a restore, leaving orphaned references (no FK backstop, per AD-005) and silently breaking 5.8's assumption that a freshly restored database yields deterministic E2E runs (`UNIQUE` collisions across runs). Catalog-driven enumeration removes the "each story must remember to register its table" failure mode. (Independent review, finding A3.)
- **Trade-off**: Restore is more destructive — it also clears execution history and idempotency keys. That is the correct semantics for a demo reset; the DW-002 guard (no restore while a non-terminal execution exists) still applies.
- **Scope**: `aplicacao/restauracao.py` + `adaptadores/persistencia/semeador.py` (extended by 2.1 T14), and allowlist maintenance in any future story that adds a versioned-config table.
- **Date**: 2026-08-30
- **Status**: active

### AD-015
- **Decision**: Adding a constraint (`UNIQUE`, `CHECK`, `NOT NULL`) or a backfilled column to an existing DuckDB table is always done by recreate-and-copy inside the migration's own transaction: `CREATE TABLE <nome>_nova (...)` with the constraints declared in the `CREATE`, `INSERT INTO ... SELECT` with explicit backfill expressions for pre-existing rows, `DROP TABLE`, `ALTER TABLE ... RENAME`. Never `ALTER TABLE ADD CONSTRAINT` (unsupported by DuckDB), and never a post-hoc `CREATE UNIQUE INDEX` for a dedup key (AD-010's insert-or-noop relies on table-constraint `ON CONFLICT` semantics; index interplay would require separate verification).
- **Reason**: Migrations `0003` (adds `UNIQUE` to `eventos_meteorologicos`) and `0005` (adds `NOT NULL` columns + `UNIQUE` to `elegibilidades_historicas`, which already holds seeded rows with no execution to reference) were unimplementable as originally written. AD-005 (no `REFERENCES` declared) is precisely what makes drop/rename inside a transaction safe. (Independent review, finding A4.)
- **Trade-off**: Migrations copy whole tables — trivial at PoC volume — and the `.sql` files get longer.
- **Scope**: Migrations `0003` (2.2) and `0005` (2.5), and every future migration that adds constraints or backfilled columns to an existing table.
- **Date**: 2026-08-30
- **Status**: active

## Handoff

- **Feature**: Épicos 2, 3, 4 e 5 (Histórias 2.1–2.6, 3.1–3.6, 4.1–4.4, 5.1–5.9) planejados; execução em andamento, história por história, seguindo a ordem de dependência.
- **Completed**: `spec.md`+`design.md`+`tasks.md` das 25 histórias (2.1–5.9) — ver auditoria de consistência cruzada e ADs no histórico desta seção. **História 2.1 (Coletar e normalizar dados do INMET) implementada e verificada PASS** (2026-09-01/02): Fix 1–4 em `552b378`, PASS confirmado em `c95279f`. **História 2.2 (Operar com segurança durante indisponibilidades meteorológicas) implementada e verificada PASS** (2026-09-02): T1–T7 `e7a3410..2a6efa8`; Verifier rodada 1 (FAIL) → Fix 1–6 `50e31fa` → rodada 2 (2 lacunas residuais de teste) → Fix 7–8 `5575981` → rodada 3 (`67ec913`) **PASS pleno — 18/18 RESIL verificados**, sensor 2/2 morto. **História 2.3 (Identificar eventos meteorológicos relevantes) implementada e verificada PASS** (2026-09-02): T1–T6 `6b2108c..22b2906`; Verifier rodada 1 achou FAIL (RISCO-09 + mutante M2 + 3 lacunas menores); Fix 1–5 em `1122288` (migração `0005` relaxa `regra_id`/`regra_versao` para `NULL`, AD-015); rodada 2 **PASS pleno — 13/13 RISCO cobertos**, sensor 3/3 morto; doc fix em `20a2b98`. **História 2.4 (Configurar, testar e versionar regras preventivas) implementada e verificada PASS** (2026-09-02): T1–T5 `73d2409..e0876e2` — `ValidadorRegra` (4 classes de invalidez, AD-013 evento↔produto), `RepositorioRegras.criar_nova_versao` (concorrência otimista, `ConflitoVersao`, mesmo padrão de `RepositorioExecucaoPreventiva`), `ServicoGestaoRegras.testar`/`.ativar` (reusa `AvaliadorRisco` de 2.3 contra cenários sintéticos já semeados, idempotente AD-002), roteador HTTP `regras` (4 rotas), superfície "Regras" (tabela versionada, formulário com erro por campo, painel de teste). Verifier rodada 1 (`e0876e2`) achou FAIL: 4 GAPs (REGRA-02/03/04/14) + 1 Partial (REGRA-10), 3/6 mutantes sobreviventes (M4 filtro `proveniencia=sintetico`, M5 off-by-one de `antecedencia_horas`, M6 ícones colapsados) + `ativar()` não chamava `avaliar` de fato, contradizendo a própria docstring/OpenAPI; Fix 1–8 em `6b33e16` (destaque: `ativar` passou a delegar a `self.testar(...)`, removendo 6 linhas duplicadas e tornando a promessa do contrato verdadeira). Rodada 2 achou 1 gap Minor residual (REGRA-04: "indicador visual" sem asserção, mutante M10 sobrevivente); Fix 9 em `c0d50d1` (6 linhas de asserção, zero produção). Rodada 3 (final) **PASS pleno — 15/15 REGRA verificados**, 15/16 mutantes mortos ao longo da história (M10 morto na correção), 340 backend + 189 frontend testes verdes; lição **L-024 promovida a `confirmed`** (2ª recorrência: distinção por texto+ícone+cor exige asserir os três sinais). **História 2.5 (Selecionar e explicar o público elegível) implementada e verificada PASS** (2026-09-02): T1–T6 `18a3547..36aafd6` — migração `0006` estende `elegibilidades_historicas` (`execucao_id` nulo=seed, `criterios`, `canal`, `UNIQUE` de dedução AD-10), `RegraSnapshot` (2.3) ganha `cobertura_exigida`, `AvaliadorElegibilidade` (5 critérios: área, tipo/situação de apólice, cobertura, participação em alertas — reusa `Criterio` de 2.3), `RepositorioCandidatosElegibilidade`/`RepositorioElegibilidades`, `ServicoAvaliacaoElegibilidade.avaliar_publico` (sem transição de estado, deferida à 2.6), roteador HTTP `elegibilidade` (quantidades + detalhe), extensão de `SuperficieEventoDecisao` com a seção "Público elegível". Verifier rodada 1 (`36aafd6`) achou FAIL grave: **1 blocker de produção real** — `obter_por_id` lançava `TypeError` em qualquer linha semeada (backfill de `criterios` na migração `0006` gravou um objeto JSON em vez de lista), fazendo a rota HTTP devolver `500` em vez de `404` — mais 3 GAPs/3 Partial de ELEG-04..09 (nome/área/canal lidos ao vivo via `JOIN`, violando AD-11; `UNIQUE` sem guarda de `apolice_id`; colunas da explicação sem asserção). Fix 1–8 em `c7c202e`: nova migração `0007` corrige o backfill (`'[]'`) e acrescenta `nome_segurado` como snapshot; `codigo_ibge_area` passa a ser derivado do próprio `criterios` já persistido, não mais via `JOIN`. Rodada 2 achou 4 lacunas residuais de asserção (nenhuma de produção); Fix 9–12 em `49bf647`, incluindo a extração de uma constante `OPERANDO_AREA_AFETADA` para eliminar por construção o acoplamento posicional entre o avaliador e o repositório. Rodada 3 (final) **PASS pleno — 10/10 ELEG verificados**, 22/35 mutantes mortos ao longo das três rodadas, 379 backend + 202 frontend testes verdes. `validate_state.py` confirma 0 erros para as cinco histórias. **Épico 2 fechado** (2026-09-02): **História 2.6 (Encerrar ou encaminhar a execução preventiva) implementada e verificada PASS**: T1–T6 `2afbaa1..1ab29ee` — migração `0008_marcos_execucao` (renumerada, mesma situação já registrada em 2.4/2.5); `RepositorioExecucaoPreventiva` ganha `listar_nao_terminais`/`registrar_marco`/`listar_marcos`/`buscar`; `ServicoColetaMeteorologica` ganha o método aditivo `coletar_para_execucao` (não altera `executar_coleta`, preservando os 19 testes de 2.1/2.2); `GerenciadorExecucoes` (o componente que o AD-7 nomeia) — `iniciar`/`continuar`/`retomar_pendentes`, encadeia coleta→risco→elegibilidade com um marco por transição, reusa `falhou_coleta` como único terminal técnico de toda a cadeia (Tech Decision testada por `test_estados_execucao.py`, não um novo valor de enum) e resolve o edge case de reinício exatamente entre o marco `publico_elegivel_formado` e a transição para `aguardando_geracao`; roteador HTTP `POST/GET /execucoes` (idempotente, prévia do público); `retomar_pendentes()` ligado ao lifespan antes do `AgendadorMeteorologico`; `SuperficieExecucao.tsx` (trilha de progresso por marcos reais, 4 categorias distintas em texto+ícone+traço, `aria-live`), reusando `SuperficieEventoDecisao` (2.3/2.5) via uma nova prop aditiva `embutido` (evita `<main>` aninhado; suíte original de 15 testes intacta). Verifier rodada 1 (única) **PASS pleno — 12/12 ACs do runner verificados**, sensor 3/3 mutantes mortos, 414 backend + 219 frontend testes verdes. 2 achados não-bloqueantes viraram lições candidatas L-025/L-026 (ver `.specs/LESSONS.md`): (a) `publico_elegivel_total`/`publico_elegivel_previa` do `GET /execucoes/{id}` são testados na API mas o frontend usa o caminho paralelo já existente (`getElegibilidade`/`getDetalheElegibilidade` via `SuperficieEventoDecisao`) — RUNNER-05 é atendido ponta a ponta, mas por uma rota de dados diferente da testada isoladamente; (b) falha técnica interna expõe código/causa/último marco mas não um texto de "impacto" dedicado (a `causa` cumpre esse papel informalmente). Nenhum dos dois é um defeito de AC; registrados para eventual limpeza futura, não bloqueiam o fechamento da história.
- **In-progress**: **Épico 3** iniciado. **História 3.1 (Preparar a produção agêntica com dados mínimos) implementada e verificada PASS** (2026-09-02/03): T1–T9 `39493d3..0f7890d` — `pyproject.toml` ganha `langchain`/`langchain-openai`/`langgraph` (versões pesquisadas e fixadas nesta história); migração `0009_preflight_ia.sql` (renumerada, mesma situação já registrada em 2.4–2.6) adiciona `execucao_origem_id` a `execucao_preventiva` e a tabela `contextos_agente`; `RetryComBackoff[T]` genérico extraído de 2.2's `ColetorComRetry` (`aplicacao/_retry.py`, comportamento de 2.2 preservado byte-a-byte); `VerificadorDisponibilidadeOpenAI` reusa o padrão de `SondaOpenAI` (Épico 1) via `GET /v1/models` — não `ChatOpenAI` — porque só o catálogo detecta "modelo configurado não existe mais" sem disparar uma chamada de geração antes do terminal (SPEC_DEVIATION aceita pelo Verifier, PREFL-03 exige exatamente isso); `MontadorContextoAgente` monta o `ContextoAgente` de 5 campos (`frozen`+`slots`, boundary de minimização testado por mutação); `RepositorioContextosAgente` persiste conteúdo + proveniência; `ServicoPreflightIA.preparar`/`.solicitar_nova_tentativa` orquestra o preflight e a retentativa correlacionada, com `RepositorioElegibilidades.copiar_para_execucao` (AD-012) copiando toda elegibilidade da origem (incluída e excluída) para a nova execução na mesma transação; roteador HTTP `preflight_ia` (3 rotas, incl. `GET /execucoes/{id}/contextos` para a proveniência, PREFL-14, adicionado além do escopo escrito da T8 mas ancorado no AC); `SuperficiePreparacaoIA` explica o bloqueio sem substituto artificial (PREFL-16/17). Verifier rodada 1 (`d4b9d8d`) achou FAIL: **1 blocker real de produção** — o hash de `Idempotency-Key` de ambas as rotas POST hashava só o corpo (sempre vazio, o alvo vem do caminho), então reusar a mesma chave em uma execução diferente devolvia silenciosamente a resposta da primeira em vez de rejeitar como conflito (Edge Case 1 da spec violado, colateral em PREFL-03/04/09) — mais 1 gap MAJOR (mutante sobrevivente: cópia de elegibilidade da retentativa, AD-012, sem asserção de fidelidade de conteúdo, só contagem/ids/flag `elegivel`). Fix em `f2d9308`: hash agora escopado por `identificador_alvo:corpo`; teste de cópia agora compara 8 campos do snapshot (nome, justificativa, canal, critérios, ids) contra a origem. Rodada 2 (`82802d3`) **PASS pleno — 16/17 PREFL com outcome de spec batido + 1 spec-precision gap (PREFL-05, já lição L-029) + 3/3 Edge Cases**, sensor 4/4 mortos (+ 2 mutações adicionais do Verifier, M6/M7, ambas mortas), 526 backend + 229 frontend testes verdes. Lições L-027–L-030 registradas na rodada 1 (nenhuma nova na rodada 2 — achado já coberto por L-027). Observação carregada adiante, não-bloqueante: `adaptadores/http/meteorologia.py` (2.2) tem a mesma forma de hash não escopado que causou o Gap 1 — fora do escopo da 3.1, mas mesmo padrão de risco se uma futura história reusar uma `Idempotency-Key` entre alvos diferentes nessas rotas.
- **História 3.2 (Gerar mensagens automaticamente para cada canal) implementada e verificada PASS** (2026-09-03): T1–T9 `801bf9a..c46ef26` (mais um fix intermediário `3121471` sobre a expectativa de contagem de migrações do inicializador). Migração `0010_mensagens.sql` (renumerada de `0008`; `0008`/`0009` já consumidas por 2.6/3.1) cria `mensagens` (estado de conteúdo do 2º diagrama do AD-4, `UNIQUE(elegibilidade_id, canal)` = dedução AD-010) e `versoes_mensagem`; `dominio/estados_mensagem.py` espelha os 9 estados do AD-4; `ValidadorSaidaCanal` valida fronteiras exatas por canal (WhatsApp 1024/SMS 160/e-mail assunto 78+corpo 2000, Unicode não bytes) com os 4 limites configuráveis em `Configuracao`; `AgenteRedator` chama `ChatOpenAI.with_structured_output` só com os 5 campos do `ContextoAgente` (3.1) — nunca id/segurado/apólice no prompt; `GrafoGeracaoMensagem` (LangGraph, 1º grafo real do projeto) encadeia `AgenteRedator` via `RetryComBackoff` (3.1) → `ValidadorSaidaCanal`; `RepositorioMensagens` (concorrência otimista, mesmo padrão de `RepositorioExecucaoPreventiva`); `ServicoGeracaoMensagens.gerar_lote` itera as elegibilidades incluídas e é idempotente via a `UNIQUE` real (reidratação não duplica nem regera); `GET /execucoes/{id}/mensagens` (só estado/canal/tentativa/metadados — o texto gerado é da revisão da 3.5, fora de escopo aqui); `SuperficieGeracaoMensagens` acompanha o progresso só por leitura. **Disparo automático (GERAR-04.1) — investigação real, não suposição do design.md**: `GerenciadorExecucoes` (2.6) nunca entra em `processando_mensagens` (`retomar_pendentes`/`_continuar` param antes); o único ponto real é `ServicoPreflightIA._preparar_agora` (3.1). `PortasPreflightIA` ganhou a porta `acionar_geracao`, chamada após a transição bem-sucedida; a composição HTTP dispara `gerar_lote` como `asyncio.Task` desacoplada (mesmo padrão do `GerenciadorExecucoes`) para o `202` do preflight não esperar o lote inteiro — 2 `SPEC_DEVIATION` (o gancho é `ServicoPreflightIA`, não `GerenciadorExecucoes`; `AgenteRedator.gerar()` devolve `RespostaRedator` com métricas de uso e saída anulável, não `SaidaCanal` puro) aceitas pelo Verifier como exigidas pelas próprias ACs (GERAR-09/10). Bug de produção achado e corrigido no caminho: `ChatOpenAI` era construído na composição (levantava `OpenAIError` sem `OPENAI_API_KEY`, impedindo o boot — contra AD-9); passou a ser construído sob demanda, na primeira geração. Verifier rodada 1 (única) **PASS pleno — 12/12 GERAR com evidência `file:line`, 11/12 com outcome de spec batido + 1 spec-precision gap (GERAR-02)**, sensor 5/5 mortos, 630 backend + 237 frontend testes verdes; 4 lições distiladas, commit `b22c061`. **3 achados não-bloqueantes (nenhum viola AC), registrados como risco em vez de corrigidos às pressas**: ver item (12) abaixo.
- **História 3.3 (Avaliar a qualidade e a segurança das mensagens) implementada e verificada PASS** (2026-09-04): T1–T6 `58d1488..edadc86`. Migração `0011_avaliacoes_criticas.sql` (renumerada de `0009`; `0009`/`0010` já consumidas por 3.1/3.2) cria `avaliacoes_criticas` (`UNIQUE(versao_mensagem_id)` = dedução AD-010, torna o replay idempotente); `AgenteCritico` avalia os 7 critérios fechados (tom, utilidade, clareza, segurança, promessa indevida, distinção de alerta oficial, adequação ao canal) recebendo só o conteúdo da mensagem + o mesmo `ContextoAgente` de 5 campos já usado pelo redator (3.1) — nenhum campo de risco/elegibilidade/cobertura/limite alcançável (CRIT-02, verificado no prompt real, não no docstring); `GrafoGeracaoMensagem` (3.2) ganha o nó `criticar`, encadeado só quando a saída do redator já era válida — roteamento estrutural, provado por um teste que afirma zero chamadas ao crítico para uma saída inválida (spy, não inferência); saída do crítico não-interpretável resolvida como `AvaliacaoCritica | None` (não um wrapper `RespostaCritico` como o do redator — decisão deliberada, sem métricas de uso a carregar, `None` nunca é lido como aprovação); `RepositorioAvaliacoesCriticas` + extensão de `ServicoGeracaoMensagens.gerar_lote` transiciona `criticando`→`aguardando_revisao` só na aprovação dupla (crítico + validação determinística de 3.2); `GET /mensagens/{id}/versoes/{id}/avaliacao-critica` marca a origem de cada veredito no próprio contrato (`origem='agente_ia'`/`'regras_deterministicas'`), não só na exibição; `SuperficieAvaliacaoCritica` distingue as três origens de decisão (agente de IA, regras fixas, decisão humana futura) em texto+ícone+cor (L-024, promovida a `confirmed` de novo — 3ª recorrência), cor exposta como propriedade CSS no próprio elemento para ser observável por teste. Verifier rodada 1 (única) **PASS pleno — 9/9 CRIT com evidência `file:line`, 2 spec-precision gaps (CRIT-06, CRIT-09)**, sensor 5/5 mortos, 689 backend + 247 frontend testes verdes; commit `3fd5b0a`. Achado do Verifier corrigido no ato (não é gap de produção, é erro de documentação): a T6 tinha registrado "630 backend (inalterado)" — resíduo da sessão que foi interrompida por rate limit; corrigido para os 689 reais em `4403b25`. **Caracterização pedida do risco (12) da 3.2** (não corrigido, por decisão deliberada — mesma razão de antes): a T4 desta história soma mais 2 pontos de chamada capazes de levantar exceção (`transicionar`) dentro do mesmo `_gerar_item` sem `try/except` amplo — 6→10 no total — mas não introduziu nenhum caminho novo desprotegido além do que já existia; a fragilidade é a mesma, só com mais superfície. 3 lições novas candidatas (L-035/036/037); L-033/L-034 (da 3.2) promovidas a `confirmed` por 2ª recorrência nesta história.
- **História 3.4 (Regenerar mensagens e registrar a proveniência agêntica) implementada e verificada PASS** (2026-09-04): T1–T6 `e303d42..70ce8af`. Migração `0012_excecoes_mensagem.sql` (renumerada de `0010`; `0009`/`0010`/`0011` já consumidas por 3.1/3.2/3.3) adiciona `mensagem_id` nulo a `excecoes_operacionais` (2.2), correlacionando exceção de mensagem sem tabela paralela; `RepositorioMensagens.incrementar_tentativa` reserva a próxima tentativa atomicamente, `LimiteTentativasExcedido` na 4ª; `GrafoGeracaoMensagem` (3.2/3.3) ganha os nós `regenerar`/`esgotar` e a aresta condicional `criticar`→`gerar` (reprovada, tentativa < 3, motivos da reprovação anterior no prompt via um 3º parâmetro opcional de `AgenteRedator.gerar` — nunca em `ContextoAgente`, que segue com 5 campos) e `criticar`→`falhou_conteudo` (3ª reprovação); `retomar_pendentes` (2.6) passa a tratar `processando_mensagens`, delegando a `ServicoGeracaoMensagens.retomar_mensagens_pendentes` — retoma cada mensagem do seu marco durável real (versão sem avaliação → critica; avaliação aprovada sem transição aplicada → completa a transição; reprovada com tentativa livre → regenera; reprovada na 3ª → esgota), nunca repete tentativa concluída nem reabre terminal; `GET /mensagens/{id}/proveniencia` expõe agente/modelo/prompt/categorias/tentativa/duração/métricas por tentativa, sanitizado. **Risco (12b) do STATE.md — fechado de verdade**: rastreado ponta a ponta (lifespan → `montar_servico_geracao` real → `retomar_pendentes` → `listar_nao_terminais` já inclui `processando_mensagens`), 9 testes dedicados. **Risco (12a) — corrigido em parte, deliberadamente**: cada item do lote/retomada agora roda dentro de um `except Exception` amplo que registra `Exceção` operacional, mesmo padrão RUNNER-11 de `GerenciadorExecucoes._processar_coleta_e_continuar` — o dano concreto (lote abortado em silêncio) está resolvido; o que não foi feito, por decisão deliberada, é inventar um terminal de execução para "falha técnica de lote" (AD-4 não tem esse estado, e a saída de `processando_mensagens` é o gate da 3.5) — a task `asyncio` desacoplada em si (`preflight_ia.py`) ainda não tem `.exception()` observado, risco permanece registrado, não removido. Verifier rodada 1 (`d4343dd`) achou FAIL: 1 gap MAJOR (mutante sobrevivente — `_retomar_grafo` lê `registro.tentativa_atual` corretamente, mas nenhum teste distinguia isso de um valor hardcoded errado, porque o único teste "sem versão ainda" seedava por acidente na tentativa 1, onde o valor errado coincide com o certo) + 1 gap Minor (branch "avaliação aprovada sem transição aplicada" sem nenhum teste). Nenhum dos dois é defeito de produção. Fix 1 (`c355fec`) e Fix 2 (`e72bbdb`), ambos só de teste. Rodada 2 (`104c12c`) **PASS pleno — 12/12 REGEN com outcome de spec batido, 3/3 Edge Cases**, sensor 8/8 mortos (incl. os 2 mutantes re-injetados da rodada 1, agora mortos), 737 backend + 253 frontend testes verdes. Achado extra da rodada 1, verificado com cuidado por não ser trivial: os 6 testes de 3.2/3.3 que afirmavam "mensagem para em `gerando`/`criticando` após reprovação" foram reescritos (não removidos, não enfraquecidos — nomes e contagem comparados commit a commit) porque aquelas duas histórias já declararam esse comportamento como deferido a esta (`spec.md` § Out of Scope de ambas). Lição L-038 (rodada 1) + L-042 candidata (rodada 2, sobre semear um teste de retomada por marco durável, não só o caso degenerado da tentativa 1).
- **História 3.5 (Revisar e decidir o lote de comunicação) implementada e verificada PASS** (2026-09-04): T1–T6 `634c8e9..50c8602`. Migração `0013_decisoes_humanas.sql` (renumerada de `0011`; `0009`–`0012` já consumidas por 3.1–3.4) cria `decisoes_humanas` (4 resultados fechados, justificativa obrigatória fora de `aprovar`); `RepositorioDecisoesHumanas` mantém a decisão humana distinta da avaliação crítica da mesma versão; `ServicoRevisaoLote.obter_lote` ordena por atenção (exceção → reprovação histórica → aprovação limpa); `.decidir_lote` aplica decisões em transação única (`TransacaoDuckDB`, componente novo — ver achado de design abaixo), com dois modos de falha genuinamente distintos: item já terminal = recusa individual sem abortar o envio; `versao_esperada` desatualizada = conflito real, aborta tudo com `409`; regeneração humana reusa a reentrada de 3.4 (`incrementar_tentativa` + volta a `gerando`/`processando_mensagens`), idempotente por chave, sem dupla contagem em reenvio; `mensagens_aprovadas` (o que segue para `aguardando_confirmacao`) exige aprovação dupla real — crítico E Marina, não presumida; `GET/POST /execucoes/{id}/revisao(/decisoes)`; `SuperficieRevisaoLote` com 3 seções separadas, bloqueio inline de justificativa, seleção múltipla. **REVISAO-01 — mesmo tipo de investigação da GERAR-04.1 (3.2)**: nada disparava a transição da execução para `aguardando_revisao` quando toda mensagem alcança um terminal de conteúdo; `ServicoGeracaoMensagens.abrir_revisao_se_lote_completo` foi adicionado, chamado ao fim de todo caminho que produz um terminal de mensagem, recomputando do estado real a cada vez (nunca contador em memória) — mesma guarda também impede o agregado de voltar cedo demais durante uma regeneração ativa (REVISAO-10). **Achado técnico de design**: uma segunda conexão DuckDB ao mesmo arquivo não enxerga a transação aberta pela primeira e sobrevive ao `ROLLBACK` dela — confirmado empiricamente pelos dois Verifiers, não só pelo autor — por isso `RepositorioMensagens`/`RepositorioExecucaoPreventiva` ganharam um parâmetro `conexao` opcional (chamadores existentes de 2.2/2.6/3.2/3.4 inalterados, default `None`). Verifier rodada 1 (`e99f4b4`) achou FAIL: 1 gap MAJOR real de produção — `retomar_mensagens_pendentes` só chamava o gatilho do REVISAO-01 dentro do laço por item retomável; se toda mensagem já estava terminal antes de um boot, o laço não iterava nenhuma e a execução ficava presa em `processando_mensagens` para sempre — mais 1 gap MAJOR só de teste (REVISAO-14: só o lado "Marina rejeitou" da dupla aprovação tinha cobertura, não "crítico nunca avaliou") e 1 Minor de documentação (3 desvios do `design.md` sem marcador `SPEC_DEVIATION`). Fix único (`0e6d557`): chamada incondicional do gatilho ao final da retomada + 2 testes novos + 3 marcadores de desvio. Rodada 2 (`aeebd0f`) **PASS pleno — 14/14 REVISAO com outcome de spec batido**, sensor 7/7 mortos (6 pela suíte, 1 — a colisão de nomes `EstadoExecucao`/`EstadoMensagem.AGUARDANDO_REVISAO`, mesmo valor de string, níveis diferentes — só pelo `pyright`, confirmando que o type checker precisa continuar obrigatório no gate), 827 backend + 268 frontend testes verdes. Lições L-043–L-046 (rodada 1); nenhuma nova na rodada 2 (achados já cobertos por L-044).
**Épico 3 fechado** (2026-09-04): **História 3.6 (Confirmar e executar a simulação sem envio real) implementada e verificada PASS**: T1–T5 `1aca779..08375aa`. Migração `0014_entregas_simuladas.sql` (renumerada de `0012`; `0009`–`0013` já consumidas por 3.1–3.5) cria `entregas_simuladas` (`UNIQUE(mensagem_id)` = dedução AD-010, sem nenhuma coluna de desfecho de provedor — a tabela por construção não tem onde inventar sucesso/falha de canal); `RepositorioEntregasSimuladas.criar_lote` copia a `apresentacao` do `conteudo` já aprovado (3.2), sem gerar nada novo; `ServicoSimulacao.confirmar` reclama atomicamente as mensagens com aprovação dupla real (crítico + Marina, recomputada do estado persistido dentro da transação — nunca do que a tela exibiu), cria as entregas e transiciona mensagens+execução numa única transação (`TransacaoDuckDB`, 3.5); falha local roda em **duas transações genuinamente distintas**: a primeira sofre `ROLLBACK` completo (mensagens voltam a `aprovada` com a `versao` nunca mutada, zero entrega parcial) antes de a segunda, idempotente, mover a execução a `falhou_simulacao` com exceção sanitizada (sem termo de canal/provedor); `solicitar_nova_tentativa` é a 3ª aplicação do padrão de execução correlacionada (2.2, 3.1), com a cópia AD-012 de elegibilidades testada especificamente contra uma origem que já chegou a `simulando` (e portanto já possui `contextos_agente`/`mensagens` que colidiriam sem a cópia); `injecao_falha_teste` é comprovadamente inalcançável em produção por um teste que faz `ast.parse` da composição HTTP, não só grep; `SuperficieSimulacao` com os 5 estados do progresso em texto+ícone+cor (L-024) e modal de reconhecimento obrigatório. Verifier rodada 1 (única) **PASS pleno — 12/12 SIMUL com evidência `file:line` e outcome de spec batido, 3/3 Edge Cases**, sensor 6/6 mortos (profundidade P0-full, justificada por ser a história de fechamento do épico), 887 backend + 285 frontend testes verdes; commit `1af79d7`. O Verifier também deu um veredito de fechamento sobre o épico inteiro: os 4 invariantes cross-cutting (minimização de dados, escopo de `Idempotency-Key` por alvo, nenhum conector externo real, terminal nunca reaberto) se mantiveram consistentes nas 6 histórias — o único "sobrevivente" conhecido é o hash de idempotência não escopado de `meteorologia.py` (2.2), já registrado como risco (11), fora do escopo de qualquer história do Épico 3. Nenhuma lição nova (achados já cobertos por L-003/L-027/L-043/L-046 existentes).
- **Épico 4 iniciado** (2026-09-04): **História 4.1 (Consolidar resultados e estados da simulação) implementada e verificada PASS**: T1–T4 `dc19fd3..2b63f5a` — T1 comprova (não precisou alterar produção) que `RepositorioMensagens.listar_por_execucao` (3.2) já devolvia `estado`+`canal` juntos; `ServicoConsolidacaoResultados` (T2, leitura pura, sem migração nova) agrega `mensagens`+`entregas_simuladas` (3.6) por execução — totais por canal/estado sobre *todas* as mensagens do lote, `nao_simulaveis` (rejeitada/excluída/em exceção) com motivo legível nunca somada às entregas, `divergencia` comparando a contagem de `simulada_entregue` com `entregas_simuladas` (nunca esperada em operação normal, reportada com correlação, nunca corrigida); enquanto a execução não chega a `concluida`/`falhou_simulacao`, os totais vêm vazios (`concluido=false`) para nunca apresentar um total parcial como final; `GET /execucoes/{id}/resultados` (T3) expõe o resultado, snapshot OpenAPI regenerado; `SuperficieResultados` (T4) — tabelas acessíveis de totais por canal/estado com cabeçalhos ordenáveis (`aria-sort`, nomes acessíveis nos botões), selo de estado distinto em texto+ícone+cor (L-024, "Enviada — simulação" nunca lida como confirmação de provedor externo), bloco de divergência com correlação/impacto, tipos gerados a partir do backend em execução (`npm run gerar-tipos-api`, diff puramente aditivo). Verifier rodada 1 (`2b63f5a`) achou FAIL: 1 gap de teste (não de produção) — o teste de ordenação da tabela de totais por estado só afirmava o atributo `aria-sort`, nunca a ordem real das linhas renderizadas; um mutante que inverteu o sinal da comparação em `ordenarTotais` sobreviveu ao sensor. Fix 1 (`27f9968`, só teste) passou a comparar o texto das linhas antes/depois de cada clique, nas duas direções. Rodada 2 (`276870f`) **PASS pleno — 10/10 RESULT verificados**, sensor 3/3 mortos (2 backend inalterados da rodada 1 + o mutante de ordenação agora morto), 904 backend + 296 frontend testes verdes. Nota de spec-precision não bloqueante carregada adiante: a cláusula "ações de linha" do RESULT-08 é vacuamente satisfeita — esta superfície ainda não tem nenhuma ação por linha (detalhe individual é da História 4.2). Lição L-050 registrada como candidata (rodada 1): "afirmar a ordem real das linhas renderizadas ao testar ordenação, não só o atributo `aria-sort`".
- **História 4.2 (Inspecionar o resultado individual) implementada e verificada PASS** (2026-09-04): T1–T3 `432de2b..1647699`. `ServicoDetalheResultado` (T1, leitura pura, sem migração nova) junta mensagem+versões (3.2), avaliações críticas (3.3), decisões humanas (3.5), entrega simulada (3.6) e elegibilidade+evento+regra (2.5/2.2) por `mensagem_id`, validando que ela pertence à `execucao_id` informada antes de devolver qualquer dado — mensagem de outra execução e mensagem inexistente devolvem o mesmo `None` (DETALHE-05, AD-011); cada versão fica relacionada à sua própria avaliação crítica e decisões humanas, na ordem cronológica das tentativas (DETALHE-04). 2 `SPEC_DEVIATION` do `design.md`, ambas verificadas como corretas pelo Verifier: evento completo vem de `RepositorioEventosMeteorologicos` (3.6) e a versão da regra já é campo direto de `RegistroElegibilidade` (2.5) — não de `RepositorioAvaliacoesRisco`, que só traz critérios de relevância meteorológica não pedidos por nenhum AC; `RepositorioExcecoesOperacionais` ganhou `obter_por_mensagem` (só tinha `registrar`) para o Edge Case de mensagem em `falhou_conteudo`/`falhou_integracao_ia` mostrar a exceção associada. `GET /execucoes/{id}/mensagens/{id}/detalhe` (T2) expõe o detalhe, `404` idêntico para inexistente e para "de outra execução". `SuperficieDetalheResultado` (T3) — drawer somente leitura com prévia do canal sempre rotulada como simulação (nunca reproduz uma entrega real), sem nenhum campo de telefone em nenhuma camada do DTO; foco preso e `Esc`/retorno de foco reimplementam o mesmo mecanismo do `Modal` (Épico 1) em vez de o envolver, porque o rodapé confirmar/cancelar e o contrato objeto/impacto do `Modal` não se aplicam a uma consulta somente leitura — decisão deliberada, não um defeito, confirmada pelo Verifier; estado "Não encontrado" com ação segura de retorno (botão Fechar). Verifier rodada 1 (única, com uma interrupção por erro de API retomada no meio do sensor — árvore real verificada limpa antes de continuar) **PASS pleno — 8/8 DETALHE verificados, 0 spec-precision gaps**, sensor 3/3 mortos (mais 1 mutação exploratória fora da cota, também morta — o guard do `estado in (FALHOU_CONTEUDO, FALHOU_INTEGRACAO_IA)` na leitura da exceção é comprovadamente redundante dado o invariante de `geracao_mensagens.py::esgotar_tentativas`, registrado como nota Minor de qualidade, não um gap bloqueante), 916 backend + 305 frontend testes verdes; commit `3f69346`. Lição **L-051** candidata (guard redundante sem teste dedicado da branch). Achado Minor não-bloqueante carregado adiante: `SuperficieDetalheResultado.tsx` duplica o mecanismo de foco preso/`Esc` do `Modal.tsx` em vez de extrair um hook compartilhado — a diferença de contrato (confirmar/cancelar vs. somente leitura) justifica não reusar o componente inteiro, mas a lógica de foco em si não depende desse contrato e poderia ser compartilhada; nenhuma história ainda agenda essa extração.
- **História 4.3 (Registrar a primeira visualização do comunicado) implementada e verificada PASS** (2026-09-05): T1–T5 `477a27a..135d90d`. Migração `0015_visualizacoes_comunicado.sql` (renumerada de `0013`; `0013`/`0014` já consumidas por 3.5/3.6) cria `visualizacoes_comunicado` (`UNIQUE(entrega_simulada_id)` = dedução AD-010, `segurado_id` snapshot). **Achado empírico real, não suposição do design.md**: `RepositorioVisualizacoesComunicado.registrar_primeira_visualizacao` provou, com threads Python reais (não só simulação), que `INSERT ... ON CONFLICT DO NOTHING` sozinho **não** absorve uma corrida genuína entre duas conexões DuckDB — a transação perdedora recebe `ConstraintException` (no próprio `INSERT`) ou `TransactionException` (só no commit implícito), dependendo do instante exato; o método agora envolve o `INSERT` em `contextlib.suppress(...)` das duas exceções e sempre segue para o `SELECT`, que converge para a linha do vencedor nos dois casos — verificado com 0 falhas em centenas de corridas reais (autor: dezenas; Verifier, independentemente: 480 chamadas concorrentes, 0 exceções não tratadas, 0 divergência). `ServicoVisualizacaoComunicado` (T3) resolve entrega+mensagem+elegibilidade por `entrega_simulada_id`, valida pertencimento ao `segurado_id` antes de devolver qualquer dado (mesmo padrão de não-enumeração de 4.2) e só grava quando a mensagem de origem está em `simulada_entregue`; `registrar_visualizacao` recebe `segurado_id` e devolve `Optional` (SPEC_DEVIATION: GET e POST são chamadas HTTP separadas, o POST não pode presumir que o GET já validou o pertencimento). `RepositorioEntregasSimuladas` ganha `obter_por_id` (só tinha `listar_por_execucao`). `GET/POST /segurados/{id}/comunicados/{id}(/visualizacao)` (T4) — sem `Idempotency-Key`, deliberadamente: a operação já é idempotente por construção via a `UNIQUE`, sem corpo variável que justifique a cerimônia de `chaves_idempotencia` (nem `tasks.md` nem `design.md` exigem o cabeçalho aqui). `SuperficieComunicado` (T5) dispara o `POST` de visualização num efeito separado, só depois do `GET` renderizar com sucesso — nunca junto do `GET` —, mostra conteúdo/canal/rótulo de simulação sem nenhuma ação administrativa, e um selo "Não confirmada" consultável com nova tentativa idempotente enquanto a visualização não é confirmada, nunca antecipando "Visualizada no portal". Verifier rodada 1 (única) **PASS pleno — 7/7 VISU verificados**, sensor 3/3 mutantes mortos, confirmou de forma independente o achado de concorrência com seu próprio teste de estresse (37 `ConstraintException` reais em 60×8 threads sem a correção), 940 backend + 312 frontend testes verdes; commit `44cefdd`. 2 lições novas candidatas: mesmo padrão de `ON CONFLICT DO NOTHING` sem guarda de exceção encontrado, ainda não verificado sob concorrência real, em `repositorio_meteorologia.py`, `repositorio_elegibilidade.py` e `repositorio_avaliacoes_criticas.py` (pré-existentes, fora do escopo desta história); declarar checagem de pertencimento em todo ponto de entrada HTTP alcançável de forma independente no `design.md`, não só num deles. Lição L-013 (portas descobertas durante a implementação devem voltar ao `design.md`) promovida a `confirmed` por recorrência.
**Épico 4 fechado** (2026-09-05): **História 4.4 (Consultar a linha do tempo ponta a ponta) implementada e verificada PASS**: T1–T4 `f60a11b..52a78c1`. `ServicoLinhaDoTempo.montar` (T1) agrega até 9 fontes por `execucao_id` — `marcos_execucao` (2.6), avaliação de risco/regra (2.3), elegibilidade (2.5), geração/crítica/decisão humana por mensagem (3.2/3.3/3.5), exceções de execução e de mensagem (2.2/3.4), simulação e visualização (3.6/4.3) — normaliza cada linha para `MarcoLinhaDoTempo` (timestamp/ator/ação/resultado/correlação/tipo) e ordena por timestamp; nenhuma tabela nova. **SPEC_DEVIATION real, verificada duas vezes (autor e Verifier) direto na migração**: `sincronizacoes_meteorologicas`/`tentativas_coleta_meteorologica` (2.1/2.2), que o `design.md` listava como fontes "por execucao_id", **não têm essa coluna** — `Sincronizacao` é por área monitorada, sem vínculo a uma execução específica; o marco real e correlacionado da etapa de coleta já existia em `marcos_execucao` (`coleta_concluida`/`falhou_coleta`, registrados por `GerenciadorExecucoes`), que é a fonte usada de fato. `RepositorioExcecoesOperacionais` ganha `listar_por_execucao` (T1) e `RepositorioExecucaoPreventiva` ganha `listar_todas` (T2, para `buscar_execucoes` filtrar por segurado/canal/estado combináveis, aceitando o custo de N+1 já registrado como risco (3) desde o planejamento). `GET /execucoes/{id}/linha-do-tempo` e `GET /execucoes` (T3) expõem os dois; `SuperficieLinhaDoTempo` (T4) agrupa marcos por mensagem em `<details>`/`<summary>` nativos (estado expandido anunciado pelo navegador, sem ARIA manual), horário localizado com o valor UTC canônico em `title`, rolagem interna com `role="region"`+`aria-label`+`tabIndex`. Verifier rodada 1 (única) **PASS pleno — 11/11 TIMELINE verificados, 0 spec-precision gaps**, sensor P0-full 5/5 mutantes mortos (profundidade justificada por fechar o épico, mesmo padrão da 3.6), confirmou de forma independente que o teste de ordenação de "3 tentativas" força a ordem via `UPDATE... SET criado_em` real (não sorte de velocidade de inserção), 963 backend + 320 frontend testes verdes; commit `556bbdb`. Nenhuma lição nova (PASS limpo). **Veredito de fechamento do Verifier sobre o épico inteiro**: os 4 invariantes cross-cutting se mantiveram consistentes nas 4 histórias — agregação somente leitura sem tabela nova em nenhuma das quatro; padrão de não-enumeração cross-segurado/cross-execução consistente entre 4.2/4.3 (a única enumeração de 4.4, `listar_todas`, é escopada ao próprio fluxo de busca-antes-de-abrir já aprovado, não uma regressão do padrão); "Enviada — simulação" nunca sugere confirmação de provedor real em nenhuma das quatro; nenhum terminal reaberto.
- **Next step**: **Épico 4 completo** (Histórias 4.1–4.4, todas implementadas + verificadas PASS, todos os commits feitos; `git status` limpo). O usuário pediu explicitamente "todo o épico" nesta sessão — próxima decisão (iniciar o Épico 5 ou pausar) cabe ao usuário. Se e quando o Épico 5 for iniciado, primeira história é **5.1 (Compreender o alerta mais relevante na visão geral)**; mesmo protocolo desta sessão: worker de implementação (todas as tasks da história, um commit por task) → gate verde → Verifier independente automático → fix→re-verify (máx. 3 rodadas) → commit da verificação → atualizar este Handoff → próxima história. Riscos acumulados do Épico 3 seguem registrados nos itens (12a) e (9) abaixo — o usuário optou explicitamente por adiá-los antes de iniciar o Épico 4 (2026-09-04), ainda não resolvidos; o item (9) agora soma 13 superfícies órfãs (`SuperficieResultados` 4.1, `SuperficieDetalheResultado` 4.2, `SuperficieComunicado` 4.3, `SuperficieLinhaDoTempo` 4.4, além das 9 dos Épicos 2/3), nenhuma ainda ligada a `App.tsx`/`PerfilContexto.tsx` — vale considerar uma história dedicada de integração de UI antes ou durante o Épico 5, já que o volume de superfícies prontas mas inalcançáveis só cresce a cada épico.
- **Review**: revisão independente do planejamento (2026-08-30) produziu 8 achados. A1–A4 (críticos/altos) corrigidos como AD-012–AD-015. **A6/A7 permanecem abertos** (contradição spec↔design na 2.6 sobre o terminal técnico — resolvida na prática pela implementação, que reusa `falhou_coleta`, mas os documentos originais da contradição não foram retroativamente editados; critério de atribuição de `tipo` no `EventoMeteorologico` sem decisão formal, campos temporais já ancorados). **A5 resolvido pela 2.6**: `GerenciadorExecucoes.iniciar` agora cria a execução "guarda-chuva" única por ciclo (coleta→risco→elegibilidade), substituindo o padrão anterior de execuções lazy por falha isolada da 2.2 para o caminho feliz. **A8 fica moot no MVP atual**: o único tipo sintético é `granizo` (AD-013) e o único tipo real é `chuva_intensa` — como a `UNIQUE(tipo, area, periodo_inicio, periodo_fim)` inclui `tipo`, um evento sintético nunca pode colidir com um real na prática; revisitar se uma história futura introduzir um cenário sintético de `chuva_intensa`.
- **Blockers**: nenhum. Riscos conhecidos: (1) Fix 5 da 2.1 (prova ao vivo do INMET) segue pendente por falta de rede na sessão — não bloqueia histórias seguintes; (2) ~~`langchain`/`langchain-openai`/`langgraph` (3.1) sem versão fixada~~ resolvido pela 3.1 (versões pesquisadas e fixadas em `pyproject.toml`); Playwright/`@axe-core/playwright` (5.8) seguem como dependência nova sem versão fixada; (3) agregação de 4.4 lê ~9 fontes (custo aceito de PoC); (4) ferramenta de conversão Markdown→PDF (5.9) ainda não escolhida; (5) achados A6/A7 acima; (6) `EstadoSincronizacao.NORMALIZANDO` (2.1) e o estado `coletando` "em andamento" (2.2, RESIL-05) são inobserváveis em produção porque a coleta é síncrona ponta a ponta — limitação estrutural conhecida desde a Verificação rodada 2 da 2.1, reconfirmada na 2.2 e de novo na 2.6 (`retomar_pendentes` não consegue retomar uma execução presa em `coletando` no boot por falta de uma coluna `area_id` em `execucao_preventiva`, e a trata como falha técnica imediata em vez de retomável — decisão deliberada e documentada em `tasks.md` da 2.6, T3); (7) `areas_monitoradas_inmet` (tabela de configuração versionada, AD-007) não é semeada por nenhum código — confirmado durante o smoke test manual da 2.2 T7 (banco de dev real veio vazio); nenhuma história ainda decidiu como/quando essa tabela é populada fora de testes, vale endereçar antes da demonstração final; (8) L-023 (2.3, aberta por decisão): RISCO-05/06 exigem comparar "medidas, área, severidade e período", mas `RegraSnapshot` não carrega nenhum campo de janela temporal e "período" nunca é comparado — a spec não define o resultado preciso, então não há defeito a corrigir, mas uma futura história que precise comparar período terá que estender `RegraSnapshot`; (9) `SuperficieEventoDecisao`, `SuperficieExecucao`, `SuperficieRegras`, `SuperficieFonteMeteorologica`, `SuperficiePreparacaoIA` (3.1), `SuperficieGeracaoMensagens` (3.2), `SuperficieAvaliacaoCritica` (3.3), `SuperficieRevisaoLote` (3.5), `SuperficieSimulacao` (3.6), `SuperficieResultados` (4.1), `SuperficieDetalheResultado` (4.2), `SuperficieComunicado` (4.3) e `SuperficieLinhaDoTempo` (4.4) ainda não estão em `App.tsx`/`PerfilContexto.tsx` (o switch de navegação real só conhece `prontidao`/`restaurar-dados-sinteticos`/`documentacao-api`/`visao-geral`) — a orquestração ponta a ponta do backend está completa, mas a navegação real do frontend para essas treze superfícies segue pendente de uma história/tarefa própria de integração de UI, ainda não agendada em nenhum épico; (10) L-025/L-026 (2.6, candidatas): `publico_elegivel_total`/`publico_elegivel_previa` do `GET /execucoes/{id}` não são lidos pelo frontend (caminho paralelo já testado cobre o mesmo requisito) e a falha técnica interna não tem um texto de "impacto" dedicado, distinto da `causa`; (11) `adaptadores/http/meteorologia.py` (2.2) tem o mesmo formato de hash de idempotência não escopado por alvo que a 3.1 corrigiu (`f2d9308`) — pré-existente, fora do escopo da 3.1, mas mesmo padrão de risco (L-027) se uma história futura reusar uma `Idempotency-Key` entre execuções diferentes nessas rotas; (12) 3.2, 3 achados do Verifier, nenhum viola uma AC, nenhum corrigido às pressas por decisão deliberada (a correção certa exigiria uma decisão de design — não há terminal de execução no AD-4 para "falha técnica geral da geração de lote", só falhas por mensagem — que merece o mesmo rigor de implementador+Verifier de uma task normal, não um patch do orquestrador): (a) **Major** — `montar_portas_preflight`'s `acionar_geracao` dispara `gerar_lote` como `asyncio.Task` desacoplada sem `except` amplo nem callback que chame `.exception()`; um erro de infraestrutura fora do caminho do grafo (ex.: `RepositorioMensagens.transicionar` levantando `ConflitoVersaoMensagem`, uma falha de conexão) aborta o resto do lote silenciosamente — a execução fica presa em `processando_mensagens` para sempre, sem exceção operacional registrada, sem nada na superfície; diverge do padrão que `GerenciadorExecucoes._processar_coleta_e_continuar` já usa (RUNNER-11: `except Exception` amplo + `_registrar_falha_processamento`) e do próprio docstring de `gerar_lote`; a task desacoplada em si (o `asyncio.create_task` real de produção) tem cobertura zero — o teste que prova o disparo automático chama `gerar_lote` inline, não passa pelo `acionar_geracao` de `montar_portas_preflight`; (b) ~~Minor — uma execução presa em `processando_mensagens` não é retomada no boot~~ **resolvido pela 3.4** (`retomar_pendentes` agora trata `processando_mensagens`, 9 testes dedicados, rastreado ponta a ponta); (c) **Minor** — `test_prompt_leva_os_cinco_campos_do_contexto_minimo_e_nada_mais` só afirma a presença dos 5 campos permitidos, o "e nada mais" do AD-9 é garantido pela estrutura do código, não por uma asserção negativa dedicada; (13) 3.3, 2 achados do Verifier, nenhum viola AC: (a) `SuperficieAvaliacaoCritica` (3.3) é a 7ª superfície órfã, mesma pendência do item (9) acima; (b) **cosmético** — `_avaliacao_de` (crítico) rejeita reprovação sem motivos e justificativa em branco, mas aceita `aprovada=True` com `motivos` não vazios, contradizendo a própria docstring de `AvaliacaoCritica`; a interface renderizaria "Aprovada" ao lado de motivos com ícone de reprovação — precisa de uma decisão de produto (o que significa aprovar com ressalvas?), não de um patch. (14) achado da 4.3, ainda não corrigido por ser fora do escopo daquela história: `repositorio_meteorologia.py`, `repositorio_elegibilidade.py` e `repositorio_avaliacoes_criticas.py` usam `INSERT ... ON CONFLICT DO NOTHING` sem a guarda de exceção que a 4.3 precisou adicionar (`contextlib.suppress(ConstraintException, TransactionException)`) — nenhum dos três foi comprovadamente exercitado sob concorrência real (threads), então o mesmo `ConstraintException`/`TransactionException` que a 4.3 encontrou empiricamente pode se manifestar neles também se uma futura história expuser esses caminhos a chamadas HTTP genuinamente concorrentes; (15) achado da 4.3: `ServicoVisualizacaoComunicado.registrar_visualizacao` precisou de um `segurado_id` extra (SPEC_DEVIATION) porque o `design.md` só previu a checagem de pertencimento no `obter_comunicado` (GET), não em todo ponto de entrada HTTP alcançável de forma independente — vale revisar designs futuros com múltiplos endpoints sobre o mesmo recurso para declarar a checagem em cada um, não presumir que uma rota "cobre" a outra.
- **Uncommitted files**: nenhum.
- **Branch**: `main`.
